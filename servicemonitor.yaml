apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: trendstore-monitor
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: trendstore-label
  endpoints:
  - port: http
    path: /metrics
    interval: 15s